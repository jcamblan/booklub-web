<%= render(ScreenComponent.new(back_path: @read_session, title: t('.new_submission'))) do %>
  <%= render(Form::ErrorChunkComponent.new(errors: @submission.errors)) %>

  <%= render "form", read_session: @read_session %>
<% end %>
