<%= render(ScreenComponent.new(back_path: @club, title: t('.new_session'))) do %>
  <%= render(Form::ErrorChunkComponent.new(errors: @read_session.errors)) %>
  <%= render "form", read_session: @read_session %>
<% end %>
