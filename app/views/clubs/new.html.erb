<div class="self-center justify-self-center flex-col flex mx-auto w-full">
  <h1 class="text-lg font-bold text-4xl"><%= t('.new_club') %></h1>

  <%= render "form", club: @club %>

  <%= render(ButtonComponent.new(variant: 2)) { link_to t('.back_to_clubs'), root_path } %>

</div>
