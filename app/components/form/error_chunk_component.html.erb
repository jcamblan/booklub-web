<% if @errors.any? %>
  <div id="error_explanation">
    <ul>
      <% @errors.each do |error| %>
        <div class="py-2 px-3 bg-red-50 my-5 text-red-500 font-medium rounded-lg inline-block w-full">
          <%= error.full_message %>
        </div>
      <% end %>
    </ul>
  </div>
<% end %>
